RAW <{
#include<stdio.h>
#include <stdbool.h>
#include<complex.h>

void test(int *);
}>
//hello
/* there
and there
*/
fn main() -> void{
    // checking here
    string g = "hello you!"; //and here
    int a[] = {5,6,7};
    /* A multiline comment here
        for checking *
    and checking / */
    test(a);
    print(a[0],a[1],a[2]);
}
fn test(int a[]) ->void{
    a[0] = 6;
    a[1] = 7;
    a[2] = 8;
}